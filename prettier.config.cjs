module.exports = {
    printWidth: 120, // 每行代碼長度 (預設120)
    useTabs: false, // 使用tab縮排 (預設false)
    tabWidth: 2, // 每個tab多少空格 (預設2)
    singleQuote: true, // 使用單引號 (預設false)
    semi: true, // 宣告結尾使用分號 (預設true)
    trailingComma: 'none', // 多行使用拖尾逗號 (預設none)
    bracketSpacing: true, // 物件大括號間使用空格 (預設true)
    jsxBracketSameLine: false, // 多行JSX中的>放置在最後一行的結尾，而不是另起一行 (預設false)
    arrowParens: 'avoid' // 只有一個參數的箭頭函式的參數是否帶圓括號 (預設avoid)
};
